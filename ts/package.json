{
  "name": "guardrail-chatbot-jev",
  "version": "1.0.0",
  "description": "Content guardrails for AI chatbots, decided by the Jev decision model",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./policies/*": "./dist/policies/*"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/policies && cp src/policies/*.json dist/policies/",
    "pretest": "tsc -p tsconfig.test.json && mkdir -p dist-test/src/policies && cp src/policies/*.json dist-test/src/policies/",
    "test": "node --test dist-test/test/*.test.js",
    "typecheck": "tsc -p tsconfig.test.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
